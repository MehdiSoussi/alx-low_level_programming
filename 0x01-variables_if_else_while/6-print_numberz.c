#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: [P213T6]"
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int c = 48;
	int r = 10;

	do {
		putchar (c);
		c++;
	} while (c <= 57);
	putchar(r);

	return (0);
}

